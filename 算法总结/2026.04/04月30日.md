# 04月30日

## 第一题：网格图中得分最大的路径 https://leetcode.cn/problems/maximum-path-score-in-a-grid/?envType=daily-question&envId=2026-04-30

**思路：网格图dp。定义dfs(i, j, t)表示从(0, 0)到达(i, j)位置，花费不超过t时，能够获得的最大分数，答案就是dfs(m - 1, n - 1, k)**

**代码：**
```python
class Solution:
    def maxPathScore(self, grid: List[List[int]], k: int) -> int:
        cost = {0: 0, 1: 1, 2: 1}
        val = {0: 0, 1: 1, 2: 2}
        m, n = len(grid), len(grid[0])
        @lru_cache(maxsize=None)
        def dfs(i, j, t):
            if i < 0 or j < 0 or t < 0:
                return -inf
            if i == 0 and j == 0:
                return val[grid[i][j]] if t >= 0 else -inf
            return max(dfs(i - 1, j, t - cost[grid[i][j]]), dfs(i, j - 1, t - cost[grid[i][j]])) + val[grid[i][j]]
        ans = dfs(m - 1, n - 1, k)
        dfs.cache_clear()
        return ans if ans > -inf else -1
```

## 第二题：整数域二分 https://www.nowcoder.com/practice/d483ab6bf19245518730a75c6ea108ae?channelPut=tracker2

**思路：板子题，就是利用排序 + 二分，其区间长度就是答案**

**代码：**
```python
import sys
from bisect import bisect_left, bisect_right

input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    n, q = MII()
    a = LII()
    a.sort()
    out = []
    for _ in range(q):
        l, r = MII()
        out.append(bisect_right(a, r) - bisect_left(a, l))
    print('\n'.join(map(str, out)))

t = 1
# t = II()
for _ in range(t):
    solve()
```
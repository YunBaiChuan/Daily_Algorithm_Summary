# 03月16日

## 第一题：三视图 https://www.nowcoder.com/practice/4f28339be67f422ca2fbe459704e8065?channelPut=tracker2

**思路：模拟。对应视图就把放对应的坐标，然后要注意一下主视图和左视图由于坐标是上下颠倒的，因此需要在输出的时候上下翻转一下**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    x, y, z, n = LII()
    f = [['.'] * x for _ in range(y)]
    l = [['.'] * z for _ in range(y)]
    u = [['.'] * x for _ in range(z)]

    for _ in range(n):
        a, b, c = LII()
        a -= 1; b -= 1; c -= 1
        f[b][a] = 'x'
        l[b][c] = 'x'
        u[c][a] = 'x'

    for i in range(y - 1, -1, -1):
        print(''.join(f[i]) + ' ' + ''.join(l[i]))

    print()

    for i in range(z):
        print(''.join(u[i]))

t = 1
# t = II()
for _ in range(t):
    solve()
```
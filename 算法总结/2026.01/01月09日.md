# 01月09日

## 第一题：二叉树的最近公共祖先（LCA）https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/description/

**思路：非常经典的板子题，具体流程如下：dfs二叉树，如果遇到None/p/q中的一个，就可以直接返回了。为什么？None自然不用说，遇到p/q中的一个能返回是因为p和q的两种位置关系，一是包含，而是非包含。先说第一种情况，如果是包含关系，那我们遇到一个之后进行返回，另外一边必然返回空，最终返回非空的那一方，不断传递向上，最终返回p/q；第二种非包含关系，我们分别遇到p和q，到某一点的时候root的left与right汇聚，他就是lca，最终与空相比，取不空的lca，不断向上传递。因此这个代码能够适应这两种情况，是非常标准且优雅的lca扳子**

**代码：**
```python
class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':
        if root in (None, p, q):
            return root 
        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)
        if left and right:
            return root
        return left or right
```

## 第二题：牛牛喜欢字符串 https://www.nowcoder.com/practice/2db5f4ee8a924dc4abefafa69c8105d5?channelPut=tracker2

**思路：贪心，我们需要最少的操作次数，那就可以cnt出每个字符串对应位置的元素个数。并且要所有对应位置的字符都变成最大数量的那个字符，具体是什么字符我们不关心。那么剩下需要修改的字符数量就是n // k - mx，我们遍历一遍，累加即可得到ans**

**注意：具体实现的时候不需要把具体的n // k个字符串构造出来，直接用step遍历即可，步长为k**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def GMI():
    return map(lambda x: int(x) - 1, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

def solve():
    n, k = MII()
    s = I()

    ans = 0
    for i in range(0, k):
        cnt = [0] * 26
        for j in range(i, n, k):
            x = ord(s[j]) - ord('a')
            cnt[x] += 1
        mx = max(cnt)
        ans += n // k - mx
    print(ans)

t = 1
# t = II()
for _ in range(t):
    solve()
```
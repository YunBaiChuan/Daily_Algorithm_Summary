# 12月6日

## 第一题：3578.统计极差最大为k的分割方式数 https://leetcode.cn/problems/count-partitions-with-max-min-difference-at-most-k/description/?envType=daily-question&envId=2025-12-06

**思路：划分dp + 单调队列优化**

**代码：**
```python
class Solution:
    def countPartitions(self, nums: List[int], k: int) -> int:
        MOD = 1_000_000_007
        n = len(nums)
        min_q = deque()
        max_q = deque()
        f = [0] * (n + 1)
        f[0] = 1
        sum_f = 0  # 窗口中的 f[i] 之和
        left = 0

        for i, x in enumerate(nums):
            # 1. 入
            sum_f += f[i]

            while min_q and x <= nums[min_q[-1]]:
                min_q.pop()
            min_q.append(i)

            while max_q and x >= nums[max_q[-1]]:
                max_q.pop()
            max_q.append(i)

            # 2. 出
            while nums[max_q[0]] - nums[min_q[0]] > k:
                sum_f -= f[left]
                left += 1
                if min_q[0] < left:
                    min_q.popleft()
                if max_q[0] < left:
                    max_q.popleft()

            # 3. 更新答案
            f[i + 1] = sum_f % MOD

        return f[n]
```

## 第二题：游游的数值距离 https://www.nowcoder.com/practice/045316e96f134d539b228f34e00903bb?channelPut=tracker2

**思路：感觉算是一道模拟题，按照他说的去做就行。有两个细节吧，一个就是x是阶乘，所以说枚举x明显比枚举y的时间复杂度更好；第二个就是y不能只取下限，因为题目说的是绝对值，因此还需要取上限。还有的话就是x与y都不能取2，这题放比赛里面估计很难完全通过**

**代码：**
```python
import sys
input = lambda: sys.stdin.readline().strip()

import math
inf = 10 ** 18

def I():
    return input()

def II():
    return int(input())

def MII():
    return map(int, input().split())

def GMI():
    return map(lambda x: int(x) - 1, input().split())

def LI():
    return input().split()

def LII():
    return list(map(int, input().split()))

def LFI():
    return list(map(float, input().split()))

fmax = lambda x, y: x if x > y else y
fmin = lambda x, y: x if x < y else y
isqrt = lambda x: int(math.sqrt(x))

'''

'''

a = [1]
for i in range(1, 50):
    a.append(a[-1] * i)

def solve():
    n = II()
    p = q = 0
    ans = inf
    for i in range(1, 50):
        if a[i] == 2:
            continue

        x = a[i] - 1
        if x == 0:
            p, q = 1, 1
            ans = n
            continue

        low_y = n // x
        high_y = low_y + 1
        res1 = abs(x * low_y - n)
        res2 = abs(x * high_y - n)

        if low_y != 2 and high_y != 2:
            y = low_y if res1 < res2 and low_y != 2 else high_y
            res = res1 if res1 < res2 and low_y != 2 else res2
        elif low_y != 2:
            y = low_y
            res = res1
        else:
            y = high_y
            res = res2

        if y != 2 and ans > res:
            p, q = i, y
            ans = res
    print(p, q)

t = 1
for _ in range(t):
    solve()
```
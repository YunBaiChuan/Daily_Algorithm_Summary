# 04月01日

## 第一题：机器人碰撞 https://leetcode.cn/problems/robot-collisions/description/?envType=daily-question&envId=2026-04-01

**思路：对于这种碰撞问题来说，还是要想到用栈解决。一般来说就是：栈中存放向右的，然后i向右遍历，不断模拟。本题是引入了一个idx数组，方便后续的处理，可以好好学习一下**

**代码：**
```python
class Solution:
    def survivedRobotsHealths(self, positions: List[int], healths: List[int], directions: str) -> List[int]:
        idx = sorted(range(len(positions)), key=lambda x: positions[x])

        st = []
        for i in idx:
            if directions[i] == 'R':
                st.append(i)
                continue 
            while st:
                j = st[-1]
                if healths[j] > healths[i]:
                    healths[i] = 0
                    healths[j] -= 1
                    break 
                if healths[j] == healths[i]:
                    healths[i] = 0 
                    healths[j] = 0 
                    st.pop()
                    break 
                healths[i] -= 1
                healths[j] = 0 
                st.pop()

        return [h for h in healths if h > 0]
```